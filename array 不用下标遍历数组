public class TestArray {
   public static void main(String[] args) {
      double[] myList = {1.9, 2.9, 3.4, 3.5};
 
      // 打印所有数组元素
      for (double element: myList) {   //for(数据类型 element：数组名)
         System.out.println(element);   //输出 element
      }
   }
}
